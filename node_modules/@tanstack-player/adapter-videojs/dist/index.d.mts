import { EngineAdapter, EventBridgeFn, EngineAdapterOptions, PlayerSource } from '@tanstack-player/core';

/**
 * Video.js engine adapter.
 * Bridges the TanStack Player controller ↔ Video.js player instance.
 */
declare class VideoJsAdapter implements EngineAdapter {
    private player;
    private bridge;
    private videoElement;
    setEventBridge(bridge: EventBridgeFn): void;
    init(container: HTMLElement, options: EngineAdapterOptions): void;
    play(): void;
    pause(): void;
    seek(seconds: number): void;
    setVolume(value: number): void;
    setMuted(muted: boolean): void;
    setPlaybackRate(rate: number): void;
    requestFullscreen(): void;
    exitFullscreen(): void;
    loadSource(src: PlayerSource | PlayerSource[]): void;
    getCurrentTime(): number;
    getDuration(): number;
    getVolume(): number;
    getPlaybackRate(): number;
    isFullscreen(): boolean;
    destroy(): void;
    private bindEvents;
    private emitBridge;
    private normalizeSources;
    private inferType;
}
/**
 * Factory function for creating a Video.js adapter.
 */
declare function createVideoJsAdapter(): VideoJsAdapter;

export { VideoJsAdapter, createVideoJsAdapter };
