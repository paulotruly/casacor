import { TanStackPlugin, PlayerContext } from '@tanstack-player/core';

/** Bookmark data model */
interface Bookmark {
    id: string;
    time: number;
    label?: string;
    createdAt: number;
}
/** Events emitted by the bookmark plugin */
declare const BookmarkEvent: {
    readonly ADDED: "bookmark:added";
    readonly REMOVED: "bookmark:removed";
};
/**
 * Bookmark plugin — stores timestamps with optional labels,
 * allows seeking to bookmarks, and emits events.
 */
declare class BookmarkPlugin implements TanStackPlugin {
    readonly name = "bookmark";
    private ctx;
    private bookmarks;
    private idCounter;
    setup(ctx: PlayerContext): void;
    destroy(): void;
    /** Add a bookmark at the current playback position (or at a specific time) */
    addBookmark(label?: string, time?: number): Bookmark;
    /** Remove a bookmark by ID */
    removeBookmark(id: string): boolean;
    /** Get all bookmarks sorted by time */
    getBookmarks(): Bookmark[];
    /** Seek to a specific bookmark */
    seekToBookmark(id: string): boolean;
}
/**
 * Factory function for creating a bookmark plugin instance.
 */
declare function createBookmarkPlugin(): BookmarkPlugin;

export { type Bookmark, BookmarkEvent, BookmarkPlugin, createBookmarkPlugin };
